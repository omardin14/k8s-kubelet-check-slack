# Kubernetes Kubelet Security Check Configuration
# Copy this file to config.yaml and fill in your actual values
# config.yaml is in .gitignore and will NOT be committed

slack:
  bot_token: "xoxb-your-slack-bot-token-here"
  channel: "#kubelet-check"
  default_channel: "#general"

kubernetes:
  namespace: "kubelet-check"
  output_dir: "/tmp/kubelet-check-results"
  max_wait_time: 300

docker:
  username: "your-dockerhub-username"
  image_name: "kubelet-check-slack"
  image_tag: "latest"

openai:
  api_key: "sk-your-openai-api-key-here"  # Optional: For AI-powered risk analysis
  enabled: true
  model: "gpt-4"  # or "gpt-3.5-turbo" for lower cost

app:
  debug: false
  test_mode: false
  log_level: "INFO"

